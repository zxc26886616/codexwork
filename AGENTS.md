# 项目规则

本仓库用于游戏开发、小游戏原型和独立网页游戏实验。后续修改请优先遵守下面规则。

## 目录约定

- 可运行游戏项目放在 `projects/<project-name>/`。
- 项目设计文档放在 `docs/projects/<中文项目名>.md`。
- 通用文档放在 `docs/`。
- `docs/` 目录下的 Markdown 文件必须使用中文文件名，例如 `项目总览.md`、`开发规范.md`、`信号漂移.md`。
- 新游戏优先从 `docs/templates/游戏设计文档模板.md` 复制一份设计文档。
- 根目录只保留仓库入口文件和全局配置，不直接放游戏源码。

## 项目文件约定

每个轻量网页小游戏默认使用下面结构：

```text
projects/<project-name>/
  README.md
  index.html
  styles.css
  game.js
  assets/
```

如果项目继续变复杂，再拆分 `src/`、`assets/`、`levels/` 等目录。

## 开发原则

- 先保证游戏可玩，再优化视觉、音效和代码结构。
- 游戏首屏应直接进入可玩体验，不做营销式落地页。
- 独立小游戏默认不依赖后端。
- 浏览器打开项目内 `index.html` 应能运行。
- UI 文案默认使用中文。
- 玩家可见操作、分数、状态、失败提示必须清楚。
- 移动端至少保证画面不溢出，核心按钮不互相遮挡。

## 代码规则

- HTML、CSS、JavaScript 保持简单直接，避免过早引入构建工具。
- 游戏主循环使用 `requestAnimationFrame`。
- 重要状态集中在初始化函数中，例如 `createState()`。
- 重要数值要可搜索、可调整，不要散落在难以理解的表达式里。
- Canvas 绘制逻辑和游戏更新逻辑尽量分开。
- 修改 `game.js` 后运行 `node --check projects/<project-name>/game.js`。

## 文档规则

- 每个游戏至少有一份设计文档。
- `docs/` 下新增 `.md` 文件时，文件名必须使用中文；项目 slug 可以继续在 `projects/` 代码目录中使用英文。
- 设计文档应包含：一句话概念、核心体验、操作方式、规则、胜负条件、主要数值、后续想法。
- 新增项目后同步更新根目录 `README.md` 和 `docs/项目总览.md`。
- 重大玩法调整后同步更新对应设计文档。

## Git 规则

- 不强推覆盖远端历史，除非用户明确要求。
- 不删除用户已有文件，除非用户明确要求或它们已经被迁移到新位置。
- 提交信息优先使用简短中文，例如 `新增信号漂移原型`、`整理项目目录结构`。
